# Database Schema

> Generated by [`prisma-markdown`](https://github.com/samchon/prisma-markdown)

- [Agent](#agent)
- [Chat](#chat)
- [User](#user)
- [Overview](#overview)

## Agent

```mermaid
erDiagram
"agents" {
  String id PK
  String name
  String model_id
  String instruction
  String enabled_tools
  DateTime created_at
  DateTime updated_at
}
```

### `agents`

AIエージェント設定

Properties as follows:

- `id`:
- `name`:
- `model_id`:
- `instruction`:
- `enabled_tools`:
- `created_at`:
- `updated_at`:

## Chat

```mermaid
erDiagram
"conversations" {
  String id PK
  String user_id FK
  DateTime created_at
  DateTime updated_at
}
"messages" {
  String id PK
  String conversation_id FK
  MessageRole role
  String content
  DateTime created_at
}
"messages" }o--|| "conversations" : conversation
```

### `conversations`

チャット会話セッション

Properties as follows:

- `id`:
- `user_id`:
- `created_at`:
- `updated_at`:

### `messages`

チャットメッセージ

Properties as follows:

- `id`:
- `conversation_id`:
- `role`:
- `content`:
- `created_at`:

## User

```mermaid
erDiagram
"users" {
  String id PK
  String email
  DateTime created_at
  DateTime updated_at
}
```

### `users`

ユーザー

Properties as follows:

- `id`:
- `email`:
- `created_at`:
- `updated_at`:

## Overview

```mermaid
erDiagram
"agents" {
  String id PK
  String name
  String model_id
  String instruction
  String enabled_tools
  DateTime created_at
  DateTime updated_at
}
"conversations" {
  String id PK
  String user_id FK
  DateTime created_at
  DateTime updated_at
}
"messages" {
  String id PK
  String conversation_id FK
  MessageRole role
  String content
  DateTime created_at
}
"users" {
  String id PK
  String email
  DateTime created_at
  DateTime updated_at
}
"conversations" }o--|| "users" : user
"messages" }o--|| "conversations" : conversation
```

### `agents`

AIエージェント設定

Properties as follows:

- `id`:
- `name`:
- `model_id`:
- `instruction`:
- `enabled_tools`:
- `created_at`:
- `updated_at`:

### `conversations`

チャット会話セッション

Properties as follows:

- `id`:
- `user_id`:
- `created_at`:
- `updated_at`:

### `messages`

チャットメッセージ

Properties as follows:

- `id`:
- `conversation_id`:
- `role`:
- `content`:
- `created_at`:

### `users`

ユーザー

Properties as follows:

- `id`:
- `email`:
- `created_at`:
- `updated_at`:
